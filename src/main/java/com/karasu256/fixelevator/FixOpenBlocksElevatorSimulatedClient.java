package com.karasu256.fixelevator;

import net.neoforged.api.distmarker.Dist;
import net.neoforged.fml.ModContainer;
import net.neoforged.fml.common.Mod;

/**
 * Client-side entry point of the FixElevator compatibility mod.
 */
@Mod(value = FixOpenBlocksElevatorSimulated.MODID, dist = Dist.CLIENT)
public class FixOpenBlocksElevatorSimulatedClient {
    public FixOpenBlocksElevatorSimulatedClient(ModContainer container) {
    }
}
